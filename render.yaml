services:
  - type: web
    name: capstone-project
    env: docker
    plan: free
    healthCheckPath: /
    buildCommand: echo "Docker build happens in Dockerfile"
    startCommand: php artisan serve --host=0.0.0.0 --port=$PORT
